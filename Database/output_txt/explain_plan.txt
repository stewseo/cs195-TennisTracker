Estimated Plan:
 +----+-----------+--------+----------+----+-------------------+-------------+-------+----------------------+----+--------+------------------------------+
|  id|select_type|table   |partitions|type|possible_keys      |key          |key_len|ref                   |rows|filtered|Extra                         |
+----+-----------+--------+----------+----+-------------------+-------------+-------+----------------------+----+--------+------------------------------+
|   1|SIMPLE     |ACTOR   |{null}    |ALL |idx_actor_last_name|{null}       |{null} |{null}                | 200|   11.11|Using where                   |
|   1|SIMPLE     |Customer|{null}    |ref |idx_last_name      |idx_last_name|137    |sakila.ACTOR.last_name|   1|   10.0 |Using where; FirstMatch(ACTOR)|
+----+-----------+--------+----------+----+-------------------+-------------+-------+----------------------+----+--------+------------------------------+

Estimated Rows: 200.0
Estimated Cost:  NaN
QueryParts: 
select `ACTOR`.`FIRST_NAME`, `ACTOR`.`LAST_NAME`
from `ACTOR`
where (
  `ACTOR`.`FIRST_NAME` like 'A%'
  and (`ACTOR`.`FIRST_NAME`, `ACTOR`.`LAST_NAME`) in (
    select `Customer`.`first_name`, `Customer`.`last_name`
    from `Customer`
  )
)
SqlString: 
select `ACTOR`.`FIRST_NAME`, `ACTOR`.`LAST_NAME`
from `ACTOR`
where (
  `ACTOR`.`FIRST_NAME` like ?
  and `ACTOR`.`ACTOR_ID` in (
    select `FILM_ACTOR`.`ACTOR_ID`
    from `FILM_ACTOR`
  )
)